[
  {
    "id": "render/variable-substitution",
    "note": "No randomness; first letter already uppercase, so post-process (default) is a no-op → exact output either way.",
    "kind": "deterministic",
    "op": "render",
    "template": "Hello %name%!",
    "context": { "name": "World" },
    "expect": { "output": "Hello World!" }
  },
  {
    "id": "render/post-process-capitalizes-first",
    "note": "postProcess defaults TRUE (mirrors render()); step-8 capitalizes the first letter. Demonstrates the flag's default.",
    "kind": "deterministic",
    "op": "render",
    "template": "hello world",
    "expect": { "output": "Hello world" }
  },
  {
    "id": "render/enumeration-first",
    "note": "postProcess:false asserts the RAW pick. With the default TRUE it would post-process to 'A'.",
    "kind": "deterministic",
    "op": "render",
    "template": "{a|b|c}",
    "rng": "first",
    "postProcess": false,
    "expect": { "output": "a" }
  },
  {
    "id": "render/enumeration-last",
    "kind": "deterministic",
    "op": "render",
    "template": "{a|b|c}",
    "rng": "last",
    "postProcess": false,
    "expect": { "output": "c" }
  },
  {
    "id": "render/enumeration-sequence",
    "note": "sequence value 1 is a raw RNG return; picks the middle option. Raw output (postProcess:false).",
    "kind": "deterministic",
    "op": "render",
    "template": "{a|b|c}",
    "rng": { "sequence": [1] },
    "postProcess": false,
    "expect": { "output": "b" }
  },
  {
    "id": "render/plural-ru-few",
    "note": "Raw plural resolution; with the default post-process this would capitalize to 'Товара'.",
    "kind": "deterministic",
    "op": "render",
    "template": "{plural 2: товар|товара|товаров}",
    "locale": "ru",
    "postProcess": false,
    "expect": { "output": "товара" }
  },
  {
    "id": "render/set-name-nonascii-is-not-a-directive",
    "note": "Identifier names are ASCII by contract (#56): this #set line is NOT a directive — it renders verbatim and %имя% never becomes a variable. PHP's \\w under /u (UCP) read the name, consumed the line and expanded the reference; the fixture is what keeps the three engines on one grammar. validate/set-nonascii-name-is-malformed pins the verdict half.",
    "kind": "deterministic",
    "op": "render",
    "template": "#set %имя% = X\n(%имя%)",
    "postProcess": false,
    "expect": { "output": "#set %имя% = X\n(%имя%)" }
  },
  {
    "id": "render/ref-nonascii-stays-literal",
    "note": "The same ASCII rule on the reference side: %Имя% is not a variable, so a context entry under that key cannot reach the output.",
    "kind": "deterministic",
    "op": "render",
    "template": "(%Имя%)",
    "context": { "Имя": "Q" },
    "postProcess": false,
    "expect": { "output": "(%Имя%)" }
  },
  {
    "id": "render/set-crlf-value-carries-no-cr",
    "note": "A CRLF-terminated #set keeps its \\r OUT of the value: the grammar's value class excludes the four ECMAScript terminators and the tail eats an optional \\r. PCRE's dot-plus-$ used to force the \\r INTO the value, so every value on a Windows-authored template ended in a carriage return (#56).",
    "kind": "deterministic",
    "op": "render",
    "template": "#set %a% = X\r\n(%a%)",
    "postProcess": false,
    "expect": { "output": "\n(X)" }
  },
  {
    "id": "render/directive-after-bare-cr",
    "note": "A bare \\r ends a line for the anchors, so the #set after it IS a directive: extracted, stripped, expanded. PCRE's /m breaks on \\n alone and left the whole line as text (#56).",
    "kind": "deterministic",
    "op": "render",
    "template": "x\r#set %a% = 1\n(%a%)",
    "postProcess": false,
    "expect": { "output": "x\r\n(1)" }
  },
  {
    "id": "render/set-value-stops-at-line-separator",
    "note": "The value's right edge is a line end: the terminator after X here is an escaped U+2028, so the value is exactly 'X' and the rest of the physical line stays in the body. An engine whose value class admits the separator captures 'X<LS>y' instead (#56).",
    "kind": "deterministic",
    "op": "render",
    "template": "#set %a% = X\u2028y\n(%a%)",
    "postProcess": false,
    "expect": { "output": "\u2028y\n(X)" }
  },
  {
    "id": "render/circular-set-stays-literal",
    "note": "Render never throws on template content (#57). A self-referencing #set \u2014 invalid to every validator \u2014 still renders: expansion stops at the budget and the unresolved reference stays literal. The standalone PHP used to throw RuntimeException here and the plugin degraded to '' via a catch.",
    "kind": "deterministic",
    "op": "render",
    "template": "#set %b% = %b%\n(%b%)",
    "postProcess": false,
    "expect": { "output": "\n(%b%)" }
  },
  {
    "id": "render/mutual-circular-set-parity",
    "note": "The parity witness for the expansion budget: %a% and %b% swap on every hop, so which name survives states whether the budget is odd or even. The reference counts recursion depth 0..50 inclusive \u2014 51 hops, odd \u2014 leaving %b%; a 50-pass loop would leave %a% (#57).",
    "kind": "deterministic",
    "op": "render",
    "template": "#set %a% = %b%\n#set %b% = %a%\n(%a%)",
    "postProcess": false,
    "expect": { "output": "\n\n(%b%)" }
  },
  {
    "id": "render/circular-set-other-vars-unaffected",
    "note": "The stop is per-text, not per-render: a knot in one variable must not take the others down with it \u2014 %c% still resolves (#57).",
    "kind": "deterministic",
    "op": "render",
    "template": "#set %b% = %b%\n#set %c% = ok\n(%b%)(%c%)",
    "postProcess": false,
    "expect": { "output": "\n\n(%b%)(ok)" }
  },
  {
    "id": "render/context-var-self-reference",
    "note": "The same lenient stop when the knot arrives through the HOST context rather than a #set: a T1 value of '%b%' under the key b leaves the literal reference (#57).",
    "kind": "deterministic",
    "op": "render",
    "template": "(%b%)",
    "context": { "b": "%b%" },
    "postProcess": false,
    "expect": { "output": "(%b%)" }
  },
  {
    "id": "render/circular-set-accumulates-then-stops",
    "note": "The exact-budget pin, from the other side: each hop wraps one more literal x…y pair around the still-unresolved reference, so the output counts the hops — 51 of each, then the raw value's own pair from the final, unexpanded return. Together with the parity witness this fixes the budget at exactly 51, not merely 'some odd number' (#57).",
    "kind": "deterministic",
    "op": "render",
    "template": "#set %b% = x%b%y\n(%b%)",
    "postProcess": false,
    "expect": { "output": "\n(xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx%b%yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy)" }
  }
]
