{
  "name": "@spintax/example-worker",
  "version": "0.0.0",
  "private": true,
  "description": "Reference Cloudflare Worker — the @spintax/core API acceptance gate (§8)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "predeploy": "node ../../scripts/check-cf-account.mjs ."
  },
  "dependencies": {
    "@spintax/core": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.108.0"
  }
}
