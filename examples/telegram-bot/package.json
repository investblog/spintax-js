{
  "name": "@spintax/example-telegram-bot",
  "version": "0.0.0",
  "private": true,
  "description": "Reference Telegram bot for @spintax/core — validate + preview spintax variations (§8, M5)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@spintax/authoring-prompt": "*",
    "@spintax/core": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.108.0"
  }
}
